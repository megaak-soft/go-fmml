# CLAUDE.md

## Language
Always reply in Japanese.


## Code Cleanliness & Licensing Rules (Anti-GPL / IP Protection)
To avoid any copyright infringement or license contamination (e.g., GPL contamination) from AI training data:

1. **No Direct Copying**: Never output verbatim code, comments, or unique documentation structures that may originate from public repositories.
2. **Avoid Proprietary/GPL Patterns**: Do not replicate overly specific logic patterns, variable names, or coding "idioms" unique to GPL-licensed projects.
3. **Alternative Implementations**: Always use standard language idioms, common open-source patterns (MIT/Apache style), or write fresh algorithms from scratch to achieve the desired output with different structural logic.
4. **Cleanroom Approach**: When implementing well-known algorithms or protocols, focus purely on functional equivalence while changing the structural approach, variable naming, and implementation details.



## 開発ロードマップ & フェーズ計画

現在は **[ Phase 8 ]** を実行中です。
※ 上のフェーズが完了して承認されるまで、次のフェーズのコードは実装しないでください。

---

### Phase 1: FM音源合成コア（Golang）
* **目的**: GoによるFM音源合成エンジンの構築
            Goで作られたゲームプロジェクト内にライブラリとして当機能を組み込んでFM音源のBGMを鳴らす目的として使用する。(なので音色の発音が終わるまで処理が止まるのではなく非同期での処理とする)
            FM音源の音色を構築するパラメータ内容はYAMAHAのFM音源チップを参考とする
            Ebitengineの"github.com/ebitengine/oto/v3"の機能を使用して音声出力する
            マルチティンバーの概念を持たせ、複数パートで別々の音色を設定できるものとする(1パートあたりの最大ポリフォニックは8音とする)

* **実装内容**:
  * go-fmml/fmcoreフォルダ内に新規にgolangファイルを作成して実装してください
  * FM音源合成コア初期化メソッドを実装(当機能を実現する為に事前に初期化や初期処理の必要があれば、その処理を実装する)
  * FM音源音色の登録メソッドを実装(FM音源音色を構成するパラメータ(オシレータ、EG/エンベロープ、アルゴリズム)内容を持つ構造体とIDを引数に受けメモリに保持する)
  * FM音源発音パート設定メソッドを実装（指定発音パートで鳴らす音色ID、音量、パン定位を引数で受け、登録する)
  * FM音源音色の発音メソッドを実装(発音先パート番号,ノート番号、発音長、ベロシティーを引数で受け、FM音源発音実処理メソッドに内容を渡して発音要求する)
      以下の２つのメソッドを用意してどちらでも使える様にする
      - 発音内容を数値で記載する場合のメソッドはNoteOnRow(partID PartID, noteNumber uint8, durationMs int, velocity uint8)
      - 発音内容を抽象化して記載する場合のメソッドはNoteOn(partID PartID, noteName string noteType string, Sustain int, velocity uint8)
          noteNameは定数名で"A4","C#5"等で定義し内部でnoteNumber uint8に変換する。noteTypeは定数名で"NOTE4"（4分音符),"NOTE8_DOT"(付点8分音符)等で定義し内部でdurationMsに変換する
          SustainはnoteTypeで指定した音符に対する持続時間を指定(100なら100%、50なら50%)、各音符100%に対するdurationMsを算出元となるテンポ(120.0等)はgo-fmml/commonにテンポ値を保持するグローバル変数を定義する
      noteNameやnoteTypeの定数はgo-fmml/const配下に定数定義ファイルを作成する

  * FM音源発音コア実処理メソッドを実装(引数で受け取ったパラメータ内容で実際にFM音源合成し発音を実行する処理、FM音源オシレータ管理を行ない、同じノートで呼び出しがあった場合は発音中の音を停止して新しいノートで再発音したり、持続音発音中にも新規発音を受け付けたりポリフォニック管理を行なう、発音要求後に発音IDを発行しすぐに発音IDを戻り値で返却する(結果はすぐ返すが音自体は発音持続中))


* **完了条件（Definition of Done）**:
  * FM音源音色の登録、パート設定、発音メソッドを呼び出し、指定した音色で指定したノートが発音されること


### Phase 2: 音楽データ再生処理（Music Macro Language）とファイル入力処理
* **目的**: Phase 1のコアエンジンを叩き、MMLシーケンスデータを再生する
          ・FM音源パラメータファイルの読み込み解析を行なう機能を作成する
          ・MML（Music Macro Language）テキストファイルを読み込み解析を行いシーケンスデータに変換する機能を実装する
          ・各データを解析しメモリに読み込まれたデータ内容を元にシーケンスデータ再生機能を実装する
            (複数パートを再生しても時間軸にズレが生じないこと)


          ** FM音源パラメータファイル読み込みに関して **

          読み込んだFM音源パラメータテキスト内容を解析してFM音源コアエンジン内部データ型に変換してメモリに保持する
          (軽微なエラーなら警告を出力するが対象箇所を無視(0指定)して続行、継続不可能内容の場合はエラー終了で読み込み中止)

          ** FM音源パラメータファイル仕様 **
          YAML形式でパラメータを記載、EnvelopeParams部分は項目名は省略しカンマ区切りの文字列で表現
          コメント行は#で表現、空行やスペース行は無視する
          1つのファイルに複数音色定義可能、

          fmVoice:
            - voiceID: 0
              algorithm: 2
              feedback: 20
              operators:
                - multiple: 1
                  totallevel: 127
                  detuneCents: 0
                  envelope: "11, 10, 0.6, 4, 12"
                - multiple: 2
                  totallevel: 40
                  detuneCents: 0
                  envelope: "31, 12, 0.3, 4, 12"
                - multiple: 1
                  totallevel: 30
                  detuneCents: 0
                  envelope: "31, 10, 0.5, 4, 12"
                - multiple: 1
                  totallevel: 0
                  detuneCents: 0
                  envelope: "31, 8, 0.7, 2, 10"


          ** MMLファイル読み込みに関して **
          読み込んだMMLテキスト内容を解析してFM音源コアエンジン内部で再生できるデータに変換する(軽微なエラーなら警告を出力するが対象箇所を無視して続行、継続不可能内容の場合はエラー終了で読み込み中止)

          ** MMLファイル仕様 **

          独自形式テキストファイルで記載,コメント行は#で表現、空行やスペース行は無視する
          拡張子は.mml

          先頭行にFM音源コアエンジンMMLファイルであることを示すヘッダ文字列定義を記載
          [GoFMML File]


          曲全体にかかわるグローバルな初期設定部分には以下ヘッダ行定義を記載する
          volumeはFM音源エンジン自体のマスターボリューム設定で、0-127の間で設定する(省略時はフル音量)

          startOffset (再生開始オフセット)はシーケンス開始オフセット位置を設定する、設定する値は全音符の個数を指定(2なら全音符2個分)
            たとえば1を設定するとMMLデータ各パートの全音符1個分のノートをスルーしてその後のノートからシーケンス再生開始する(ただしノート以外のコマンドは無視せずシーケンス開始直前に内部的に設定処理は行う)
            startOffsetが未設定の場合は当処理は行わない(無効値の場合は無視)
            オフセット再生箇所で前の小節からの持続音等から開始した場合など都合が悪いパートがある場合はその部分の音は鳴らなさない


          [global]
            tempo: 120
            sequenceID: SampleMML
            loop: false
            volume: 100
            startOffset: 1


          曲を構成するチャネルパート初期設定部分には以下ヘッダ行定義を記載する
          [partSetting]

          パート設定部分はYAML形式で記載
          volumeは0-127の間で設定
          panは -16(左) 0(中央) 16(右)内で設定し たとえば -7の場合はやや左寄りの定位で鳴る

          part:
            - partNo: 1
              voiceID: 0
              volume: 100
              pan: 0
            - partNo: 2
              voiceID: 1
              volume: 127
              pan: -16


          各パートのMMLデータコマンド定義部分には以下ヘッダ行定義を記載する(part1～part16)
          [part1]


          MMLデータ自体は独自MMLデータLコマンド仕様を連結した文字列で定義する、[partN]と [partNEnd]で囲まれている部分に記載されているMMLデータコマンドは自由改行OK、文字間に空白あってもOK
            (空白、タブ、改行は読み込む際に削除してMML文字列を連結解析する)

          O3 C4D4E4F#4
          G8A8B8
          O4 C16C#16D16e16 F2R4G8


          各パートのMMLデータ定義終了部分には以下ヘッダ行定義を記載する(part1End～part16End)
          [part1End]


          ** MMLデータコマンド仕様 **
            大文字小文字を区別しない

          C D E F G A B : それぞれ、ドレミファソラシのノート

          [] : [と]に囲まれたノートはコード(和音)を発音することを表す。([の中にはノートとオクターブのみ記載を許容し、他のコマンドは無視する])　[C4E4G4]
                (和音内でオクターブが違うノートを混在記載する場合はノートの前に O でオクターブを指定する)

          # + -　:ノートの後につけて半音上げ下げを表す。#と+は同じ意味

          R : 休符（レスト）

          数字: 音符や休符の後につけ、音の長さを表す。2=2分音符。.は付点で長さを1.5倍し、4.=付点4分音符。3連符の長さの場合は 6, 12, 24で指定

          O : オクターブ指定、記載以降のノートは指定オクターブで発音する、　O4はオクターブ4

          > : 1オクターブ下げる。記載以降のノートは下がったオクターブで発音する

          < : 1オクターブ上げる。記載以降のノートは上がったオクターブで発音する

          L : C,E やRの後に数字をつけないときの音符を指定。Lの後に音符長を指定(Lのみ指定の場合は初期値は4)、記載以降のノートは指定音符長で発音する、　L4 CDREは　C4D4R4E4と同義

          Q : 音符の長さに対する発音の長さを指定する(スタッカートを表現)、Qの後に発音長を指定(Qのみ指定の場合は初期値は4)1=25%, 2=50%, 3=75%, 4=100%。記載以降のノートは指定発音長で発音する。 L C L1 D

          V : ベロシティーを指定、Vの後にベロシティ値を指定(Vのみ指定の場合は初期値は64)、記載以降のノートは指定ベロシティーで発音する、 V120 C V100 D

          P : パートの左右の定位（パン）を設定する。[partSetting]のpanと同じ -16(左) 0(中央) 16(右) のスケールで指定する（例: P-7で若干左寄り）

          @ : パートの音色IDを切り替え設定する(@の後ろに指定)　@2

          & : 同音ノート同士の連結をする、 C1 & C1 & C1 は全音符を3個連結して持続音(アタックは1回)を鳴らす(違うノートを & で連結した場合は無視される)

          |: :| パターン繰り返し。　繰り返し開始「|:」、繰り返し終了「:|」に囲まれたコマンドは囲い部分の後部に付いている「* n」の回数分を同じパターンで繰り返し演奏する。
            (例 |: L4 C D E :|*2 は L4 C D E L4 C D Eと同義)、「|: :|」の囲いの中には他コマンドをすべて記述記載可。「* n」の記載を省略した場合は 1回のみの演奏と同義。

          # : MMLデータ内容で # から始まる行はコメント行として 任意内容を記載できることとする(#の前に空白が入っていた場合は空白を無視)

* **実装内容**:
  * 各処理はGolangで作成し、それぞれ以下の格納ディレクトリに格納してください
    ・ファイル入力関係の処理ソースを格納するフォルダ　→ go-fmml/fileio
    ・ファイル内容を解析する処理ソースを格納するフォルダ　→ go-fmml/analyze
    ・シーケンスデータ再生する処理コードを格納するフォルダ　→ go-fmml/player
    ・FM音源音色データやシーケンスデータをメモリ上に保持するコードを格納する→ go-fmml/memory

  * FM音源パラメータファイル読み込みメソッドを実装(引数に絶対パスファイル文字列を持ち、指定したパスからファイルを読み込み解析メソッドを呼ぶ、警告の場合はログ出力、エラーの場合はエラーを返す)
  * FM音源パラメータファイル読み込みメソッド(embed版)を実装(引数1にgolangのembed.FS、引数2にに埋め込みパスファイル文字列を持ち、指定したパスからファイルを読み込み解析メソッドを呼ぶ、警告の場合はログ出力、エラーの場合はエラーを返す)
  * 読み込んだFM音源パラメータファイル内容を解析してFM音源コアエンジン内部データ形式に変換しgo-fmml/memory内のmapオブジェクトに格納する(プライベートメソッド)

  * MMLファイル読み込みメソッドを実装(引数に絶対パスファイル文字列を持ち、指定したパスからファイルを読み込み解析メソッドを呼ぶ、警告の場合はログ出力、エラーの場合はエラーを返す)
  * MMLファイル読み込みメソッド(embed版)を実装(引数1にgolangのembed.FS、引数2にに埋め込みパスファイル文字列を持ち、指定したパスからファイルを読み込み解析メソッドを呼ぶ、警告の場合はログ出力、エラーの場合はエラーを返す)
  * 読み込んだMMLファイル内容を解析してFM音源コアエンジンで再生できる内部シーケンスデータ形式に変換しgo-fmml/memory内のシーケンスデータ構造体オブジェクトに格納する(プライベートメソッド)
    (engine.NoteOnメソッドの引数で渡せる状態のデータ型で保持する。O4 Q2 V100 C4などの場合は (part1, "C4", Note4, 50, 100))
    シーケンスデータはメモリ上に複数保持可能(SequenceIDをキーにしてmapで管理)

  * メモリに保持しているシーケンスデータをもとに最大16パートのFM音源をリアルタイムで自動発音して曲として再生するPlayメソッドを実装(引数はSequenceID)
    Playメソッドは呼び元を待たさせない様に非同期として、呼んだらすぐ結果を返す(引数にコールバックメソッドの参照を渡し、ループなしの曲の場合は再生が終わったタイミングでコールバックメソッドを呼ぶ)
    4/4で1小節192分割したタイミング粒度で再生する
    Loop設定がtrueの場合はシーケンスデータが終了したら先頭から再度再生しなおしてループ再生する(各パートデータの中で一番長い部分を末尾とする)
    16パートFM音源を再生中も各パートの発音時間軸にズレが出ない様に管理するclaude
    シーケンスデータ中にある指定音色IDがメモリ上にない場合は代わりにデフォルト音色で鳴らす(デフォルト音色パラメータ内容はAIにお任せ)

  * 再生中のシーケンスを中断するメソッドを実装
  * 中断中のシーケンス再生を再開するメソッドを実装(中断した箇所が発音タイミング的に好ましくない場所の場合は少し巻き戻して調整して再生再開する)
  * 再生中のシーケンスを停止するメソッドを実装
  * シーケンスが再生中かどうかを返すメソッドを実装(再生中の場合はtrueを返す)
  * シーケンスの再生箇所を先頭に巻き戻すメソッドを実装(再生中に呼ぶと先頭から再生し直す、中断中に呼ぶと再開した際に先頭から再生する)
  * メモリに保持しているFM音源パラメータデータをすべて削除してメモリ解放するメソッドを実装
  * メモリに保持しているシーケンスデータをすべて削除してメモリ解放するメソッドを実装

* **完了条件（Definition of Done）**:
  * FM音源パラメータファイル、MMLファイルの読み込み、を行ない、設定しな内容で正しく曲のシーケンス再生されること
    サンプルFMパラメータファイル: sample_fm.yaml
    サンプルMMLファイル: sample1.mml



### Phase 3: PCM音色ファイルとPCM波形(WAV)ファイルの読み込み、PCMパートのシーケンス再生
* **目的**: PCM波形をメモリに読み込み、FM音源パートとは別でPCMパートにPCM波形音色を割り当て、シーケンス再生時にドラム音や生楽器音を鳴らせる様にする
          既に存在するFM音源16パートとは別に最大16パートのPCMパートを持たせる


          ** PCM音色設定ファイル読み込みに関して **

          読み込んだPCM音色パラメータテキスト内容を解析して内部データ型(PCMタイプ)に変換してメモリに保持する
          (軽微なエラーなら警告を出力するが対象箇所を無視(0指定)して続行、継続不可能内容の場合はエラー終了で読み込み中止)


          ** PCM音色設定ファイル仕様 **
          YAML形式でパラメータを記載
          コメント行は#で表現、空行やスペース行は無視する
          1つのファイルに複数音色定義可能、

          PCM音色設定タイプには以下2種類のどちらかを設定

              ・ワンショットタイプ:oneShot (ワンショットドラム音色を想定、複数のノートにそれぞれWAVサンプル波形を割り当て発音する手法
                (複数のvoiceSetting要素を記載し、noteに定義しているノートが呼ばれた際に指定WAVサンプルを再生する、最大voiceSetting数は16個まで)
                (ファイル自体にループ設定していたとしても無視してワショット再生)
                (loop設定、baseNote設定、envelope設定、は無視される)

              ・ロングタイプ: long (ヴァイオリン等の持続音色を想定、割り当てられるWAVサンプル波形は1つのみ、baseNote起点で波形音そのまま鳴り、発音する上下ノートによって再生波形ピッチも変化(12音階で)して鳴る
                (note, volume, pan 設定は無視)
                (loopをtrueに設定した状態で波形ファイル自体にループ設定が含まれていればアタック時に先頭から再生し、ループ開始・終了部分をループ再生する。ループ設定がなければ波形の先頭と末尾までをループ再生する)
                (loopをfalseに設定した場合はファイル自体にループ設定していたとしてもループ再生しない)
                (envelopeはFM音源のエンベロープと同様の設定内容と処理を行なう)
                (voiceSettingは1個限定)


              volimeは0-127の間で設定

              panは -16(左) 0(中央) 16(右)内で設定し たとえば -7の場合はやや左寄りの定位で鳴る

              fileNameに設定するWAVファイルの仕様(再生するWAV波形の仕様)はモノラル、44.1Khzのみとする

              fileNameは絶対パスで指定する(LoadPCMVoiceFileFSの場合はfsysのルートからの絶対パス)
              パス無のファイル名のみを記載した場合はPCM音色設定ファイル自体と同一フォルダをまず見に行く



          pcmVoice:
            # ワンショットタイプパラメータ例
            - voiceID: 0
              voiceType: oneShot
              loop: false
              voiceSetting:
                - fileName: kick.wav
                  note: C1
                  baseNote:
                  volume: 100
                  pan: 0
                  envelope:
                - fileName: snare.wav
                  note: E1
                  baseNote:
                  volume: 100
                  pan: 0
                -fileName: hat1.wav
                  note: F#1
                  baseNote:
                  volume: 100
                  pan: -16

            # ロングタイプパラメータ例
            - voiceID: 1
              voiceType: long
              loop: false
              voiceSetting:
                - fileName: violin.wav
                  baseNote: A3
                  envelope: "28, 14, 0.5, 3, 14"



          ** MMLファイル仕様の拡張に関して **
          今回PCMパートの音色を再生するにあたり、既存のMMLファイルを拡張し、設定やコマンド記載方法を追加する、尚既存部分の設定に関しては影響はない

          ** MMLファイル追加仕様 **

          曲を構成するPCMチャネルパート初期設定部分には以下ヘッダ行定義を記載する
          [pcmPartSetting]

          パート設定部分はYAML形式で記載
          パートNoにはFM音源パートとの差別化を図る為にAからHを設定する(最大8パート)
          volumeは[partSetting]と同じく0-127の間で設定
          panも[partSetting]と同じく -16(左) 0(中央) 16(右) の間で設定
          partTypeにoneShotを設定した場合はoneShotのPCM音色のみ設定可能
            (従来のMMLとは記載記法が異なる)
            (パート設定のpanは無視されて、音色側で設定したpanが適用される)
          partTypeにlongを設定した場合はlongのPCM音色のみ設定可能

          pcmPart:
            - partNo: A
              voiceID: 0
              partType: oneShot
              volume: 60
              pan: 0
            - partNo: B
              voiceID: 1
              partType: long
              volume: 100
              pan: 5


          各PCMパートのMMLデータコマンド定義開始部分には以下ヘッダ行定義を記載する(pcmPartA～pcmPartH)
          [pcmPartA]


          各PCMパートのMMLデータコマンド定義終了部分には以下ヘッダ行定義を記載する(pcmPartXEnd～pcmPartXEnd)
          [pcmPartAEnd]



          PCMパートのMMLデータ自体は独自MMLデータLコマンド仕様を連結した文字列で定義する、[pcmPartX]と [pcmPartXEnd]で囲まれている部分に記載されているMMLデータコマンドは自由改行OK、文字間に空白あってもOK
            (空白、タブ、改行は読み込む際に削除してMML文字列を連結解析する)


          PCMパートタイプが OneShot と long によりMMLコマンドの記載手法が異なる(partTypeによりMMLコマンド解析処理を切り替える)


          ・ワンショットタイプ:oneShot
            ワンショットPCM音色で設定したノートの後ろに{ }で囲んだ部分がそれぞれの対象ワンショットMMLコマンドを定義する、複数のノート定義を設定可能
            (コメント行は#で表現、空行やスペース行は無視する)


            ワンショットタイプで使用できるコマンド

            X : ワンショットPCM音色を発音する
              (WAV波形の最後まで再生、ただしWAV波形再生が最後まで終わってない時点で次の音が発音された場合は、最初の発音は止めて後発の発音を優先して発音する)

            R : 休符（レスト）

            数字: 音符や休符の後につけ、音の長さを表す。2=2分音符。.は付点で長さを1.5倍し、4.=付点4分音符

            L : C,E やRの後に数字をつけないときの音符を指定。Lの後に音符長を指定(Lのみ指定の場合は初期値は4)、記載以降のノートは指定音符長で発音する、　L4 CDREは　C4D4R4E4と同義

            V : ベロシティーを指定、Vの後にベロシティ値を指定(Vのみ指定の場合は初期値は64)、記載以降のノートは指定ベロシティーで発音する、 V120 X V100 X


            ワンショットタイプMMLでは @, [], # + , O, <, >, Q, P のコマンドは無効


            C1 {
              V100 L4 X R X8 X8 X
              X R X8 X8 X
            }
            E1 {
              V100 L4 R X R X
              R X R X8 X8 X
            }
            F#1 {
              L8 V70 X V50 X X X V70 X V50 X X X
              V70 X V50 X X X V70 X V50 X X X
              X
            }

          ・ロングタイプ: long
              既存のFM音源パートMMLと同じコマンド記載で演奏



* **実装内容**:
  * 各処理はGolangで作成し、それぞれ以下の格納ディレクトリに格納してください
    ・PCM音色関係ファイル入力関係の処理ソースを格納するフォルダ　→ go-fmml/fileio
    ・PCM音色パラメータファイル内容を解析する処理ソースを格納するフォルダ　→ go-fmml/analyze
    ・MMLファイル内のPCM音色パートMML設定内容を解析する処理ソースを格納するフォルダ　→ go-fmml/analyze
    ・シーケンスデータ再生する処理コードを格納するフォルダ　→ go-fmml/player
    ・PCM音色データやシーケンスデータをメモリ上に保持するコードを格納する→ go-fmml/memory
    ・WAV波形の解析、WAV波形をPCM音色パートとして発音する処理ソースを格納するフォルダ→ go-fmml/pcmcore



  * PCM音色設定パラメータファイル読み込みメソッドを実装(引数に絶対パスファイル文字列を持ち、指定したパスからファイルを読み込み解析メソッドを呼ぶ、警告の場合はログ出力、エラーの場合はエラーを返す)
      音色設定ファイル読み込み後に音色設定内のWAV波形ファイルパスを元にWAVファイルをメモリに読み込む(ファイル読み込みエラーの場合はエラーを返す)

  * PCM音色設定パラメータファイル読み込みメソッド(embed版)を実装(引数1にgolangのembed.FS、引数2にに埋め込みパスファイル文字列を持ち、指定したパスからファイルを読み込み解析メソッドを呼ぶ、警告の場合はログ出力、エラーの場合はエラーを返す)
      音色設定ファイル読み込み後に音色設定内のWAV波形ファイルパスを元にWAVファイルをメモリに読み込む(ファイル読み込みエラーの場合はエラーを返す)

  * 読み込んだPCM音色パラメータファイル内容を解析してPCMコアエンジン内部データ形式に変換しgo-fmml/memory内のmapオブジェクトに格納する(プライベートメソッド)

  * WAVファイルを読み込む処理を行なうメソッド

  * 読み込んだWAVファイル内容を解析し(ループ設定等)、再生出来る状態のデータにするメソッドを

  * 既存のMMLファイル読み込みメソッドを実装にPCM音源用に拡張追加した仕様やコマンド解析処理の追加対応を行なう

  * PCM音色のWAV波形データをメモリから読み出して発音する処理を行なうメソッドを実装する
    (ワンショットパート、ロングパートの発音やループ再生処理)

  * 既存のPlayメソッドを実行した際に、FM音源パートのタイムラインと同期して複数のPCM音色パートも演奏できる様に追加対応を行なう


* **完了条件（Definition of Done）**:
  * PCM音色パラメータファイル、MMLファイル(PCMパートMMLを含む)の読み込み、を行ない、設定した内容で正しく曲のシーケンス再生されること

    cmd/mmldemo2
    サンプルFMパラメータファイル: sample_fm.yaml
    サンプルPCM音色パラメータファイル: sample_pcm.yaml
    サンプルMMLファイル: sample2.mml
    ワンショット(バスドラム)ファイル: kick.wav
    ワンショット(スネアドラム)ファイル: snare.wav
    ワンショット(ハイハット)ファイル: hat.wav
    ロング(ヴォイス)ファイル: uhh.wav




### Phase 4: マスター出力にマスターリバーブエフェクト機能を追加して各パートの出力に対しリバーブをかける
* **目的**: ミックスされた最終出力に対して1個だけステレオリバーブ処理機能を設置し、各パートからのセンドレベルによりリバーブかけてマスター出力にミックスする
            リバーブを未使用設定の場合はバイパスする。パフォーマンス低下を考慮して軽量・中量リバーブを2種類の中から選択可能。
            リバーブの入出力はトゥルーステレオ(例えば入力の定位が右寄りだった場合はリバーブ出力も右寄りになる)
            リバーブ演算にはfloat32を使用して軽量化する
            リバーブ機能追加の為にMMLファイル仕様を拡張する


          ** MMLファイル仕様の拡張に関して **
          今回リバーブエフェクト機能追加するにあたり、既存のMMLファイルを拡張し、設定や記載方法を追加する、尚既存部分の設定に関しては影響はない

          ** MMLファイル追加仕様 **

          [global]にreverb、reverbType、reverbTime、reverbLevelのパラメータを追加(リバーブを使用しない場合は4個共に未記載でもOK)

          reverbがtrueの場合はリバーブをかける、falseの場合はリバーブをかけない(バイパス)
          reverbTypeが simple の場合はSchroeder(シュローダー)演算タイプの軽量リバーブを使用する
          reverbTypeが normal の場合はFDN(Feedback Delay Network)演算タイプのリバーブを使用する
          reverbTime に秒数(小数点第一まで設定可)を設定する 0.5 - 3.0秒 まで設定可(0はバイパスと同義) 0.5以下はバイパス、3.0以上に指定の場合3.0上限に設定
          reverbLevel は マスターのドライミックスに対するリバーブの音量を設定、0 - 127 の値で設定 (0の場合はドライ)

          [global]
            tempo: 120
            sequenceID: SampleMML
            loop: false
            volume: 100
            reverb: false
            reverbType: normal
            reverbTime: 1.5
            reverbLevel: 50


          FM音色パート　part に reverbSend項目を設定、0 - 127　の範囲で設定 (127は100%wet)

          part:
            - partNo: 1
              voiceID: 0
              volume: 100
              pan: 0
              reverbSend: 30


          PCM音色パート pcmPart のpartTypeが long の場合は FM音源パート同様に reverbSend項目を設定、0 - 127　の範囲で設定 (127は100%wet)
          PCM音色パート pcmPart のpartTypeが oneShot の場合は wav割り当てノート別に 「ノート: センドレベル」をカンマ区切りで記載しダブルクォーテーションで囲む(未記載ノート場合はリバーブをかけない)


          pcmPart:
            - partNo: A
              voiceID: 0
              partType: oneShot
              volume: 60
              pan: 0
              reverbSend: "C1: 5, E1: 30, F1#: 15"

            - partNo: B
              voiceID: 1
              partType: long
              volume:
              pan: 5
              reverbSend: 40


          各パートでリバーブセンドレベルを設定しても [global]のreverb設定がfalseｂの場合はリバーブはかからない(バイパス)

          Play中にStopやResumeした場合は 極短フェードアウトしてリバーブリリースを残さない様にする


* **完了条件（Definition of Done）**:
  * FM音色パート、PCM音色パート(long, oneShot)のセンドレベルによって想定通りにリバーブがかかること
    Schroeder(シュローダー)演算タイプ、FDN(Feedback Delay Network)演算タイプのそれぞれのリバープを設定切り替えできること



### Phase 5: FM音源の基本波形を全8波形から選べる様にする。MML機能追加
* **目的**: 従来のFM音源の基本波形(正弦波)の他に7種類を追加し、オペレータ単位で波形設定することでFM音源で鳴らせる音色のバリエーションを豊かにする
            またMMLに機能拡張を行ない、使い勝手や表現力をアップする。
              パートミュート機能の追加
              パートトランスポーズ機能の追加
              ポルタメント・ピッチベンドアップ・ピッチベンドダウンで音程を滑らかに変化させるMML記法を追加
              MML記法内容を表現できる様にFM音源、PCM音源のエンジン内容を改修する

            
          ** FM音源基本波形追加に関して **
          従来のFM音源の基本波形(正弦波)の他に7種類の基本波形を追加し、FM音源パラメータ設定で、使用する波形を選べる様にする
          
          基本波形
          sin := math.Sin(phase)

          波形8種類一覧
            W1: 正弦波（サイン波）基本波形（全周期）従来の波形
                return sin

            W2: 半波整流正弦波 正（上）の半周期のみを取り出し、負（下）を0に固定
              if sin < 0 {
                return 0
              }
              return sin


            W3: 全波整流正弦波  負（下）の半周期を正（上）へ反転
              return math.Abs(sin)*2.0 - 1.0


            W4: 半波整流正弦波（周期半分） 前半のみ正の半周期で、後半は0
              if phase < math.Pi {
                  return math.Sin(phase * 2.0)
              }
              return 0

            W5: 鋸の歯状に近い正弦波 正の半波を2倍周期（半分の幅）で2回繰り替えし
              if phase < math.Pi {
                return math.Sin(phase * 2.0)
              }
              return 0

            W6: 全波整流（周期半分） W3の山をさらに2倍の密度で詰めた形
              return math.Abs(math.Sin(phase*2.0))*2.0 - 1.0


            W7: 正の全波整流（極性あり） W3を小さくし、正側にシフトした波形
              return math.Abs(math.Sin(phase))

            W8: 非対称鋸の歯状正弦波 W5の交互反転版
              return math.Sin(phase * 2.0)



          ** FM音源音色パラメータ追加仕様 **
            fmVoice の 各operators に wave パラメータを追加 W1 ～ W8 までを文字列で設定(未設定 or 無効値の場合はデフォルトでW1を内部設定)


            fmVoice:
              - voiceID: 0
                algorithm: 4
                feedback: 20
                operators:
                  - multiple: 20
                    totallevel: 20
                    detuneCents: 0
                    wave: W1
                    envelope: "28, 10, 0.6, 4, 20"
                  - multiple: 2
                    totallevel: 40
                    detuneCents: 0
                    wave: W1
                    envelope: "31, 12, 0.3, 4, 20"
                  - multiple: 1
                    totallevel: 30
                    detuneCents: 0
                    wave: W2
                    envelope: "31, 10, 0.5, 4, 20"
                  - multiple: 1
                    totallevel: 0
                    detuneCents: 0
                    wave: W2
                    envelope: "31, 8, 0.7, 2, 20"



          ** MMLファイル仕様の拡張に関して **
            ・各FM音色パート、PCM音色パートにそれぞれミュート設定 「mute」 パラメータを追加する(尚既存部分の設定に関しては影響はない)
            ・MML演奏データ記法にポルタメント、ピッチベントアップ、ピッチベンドダウンをそれぞれ追加



          ** MMLファイル追加仕様(ミュート機能) **
             mute: true (ミュートする)
             mute: false (ミュートしない)

            (未指定 or 無効値の場合はミュートせずに鳴らす)

          ** MMLファイル追加仕様(トランスポーズ機能) **
            パートのノート音階を半音単位で転値(トランスポーズ)する設定を part、pcmPartの各パートに追加、1は単音、12はオクターブ、(-36 から 36まで指定可)
            (未指定 or 無効値の場合は無視)
  
  
            [partSetting]
            part:
              - partNo: 1
                voiceID: 10
                volume: 50
                pan: 0
                transpose: -12
                mute: true

            [pcmPartSetting]
            pcmPart:
              - partNo: A
                voiceID: 0
                partType: long
                volume: 60
                pan: 0
                transpose: 2
                mute: false


          ** MMLファイル追加仕様(MML演奏データ記法) **

          ポルタメント記法について
          ・ノートとノートの間に　&& を　記載すると　前ノートと後ノートを接合しポルタメント(ピッチベンド)で滑らかに音程移動演奏する
          ・ポルタメントの発動タイミングは後ノートの発音タイミングである
          ・ポルタメントの長さ(前ノートから後ノートの音程に完全移行するまでの時間)は && の後ろにノート音符長を指定する(&&4)
          ・ポルタメントの長さが未指定の場合(&&だけ)は、ポルタメント長は後ノートの50%の長さとする
          ・ポルタメントの音符長が後ノートの音符長を超えるなどの無効値の場合は、ポルタメント長は後ノートの50%の長さとする
          ・前ノートにスタッカートが設定されていた場合は、前ノートのスタッカートを無効にする(ポルタメントを行なう)          
          ・前ノートと後ノートの間に休符が存在している場合はポルタメントは無効とする
          ・前ノート(単音)と後ノート(コード和音)の場合は ポルタメントは無効とする
          ・前ノート(コード和音)と後ノート(単音)の場合は ポルタメントは無効とする
          ・前ノートと後ノートがそれぞれコード(和音)の場合は ポルタメント設定演奏可とする(和音の先頭ノート同士の差分音程移動をそれぞれ適用してポルタメントする)
          ・前ノートと後ノートがそれぞれコード(和音)の場合で前コード(和音)と後コード(和音)の構成数が違う場合や同音程の場合は、和音の先頭ノート同士の差分音程移動をそれぞれ適用してポルタメントする
          ・ポルタメント記法は FM音色パート、PCM音色パート設定が long 、の場合のみに設定可能で、PCM音色パートが oneShot の場合には設定不可である
          ・従来記法同様に空白、スペースは無視される

          (例)
          L2 C &&4 E

          [C E G]
          &&
          [D F G B]


          ピッチベンド記法について

          ・ノート直前に _/(ピッチベンドアップ)、_\(ピッチベンドダウン)を定義することで対象ノートの発音時にピッチベンドをかけることができる(ピッチ変化は単純線形)
          ・ノート直後に /_(ピッチベンドアップ)、\_(ピッチベンドダウン)を定義することで対象ノートの発音終了直前にピッチベンドをかけることができる(ピッチ変化は単純線形)

          ・_/(発音開始時ピッチベンドアップ)の場合は _ の後に 発音開始時のピッチ差分音数を設定できる(1が半音とし、2が全音等で指定)、例えば _2/D の場合は Cで発音後にピッチベンドアップして、最終的にDの音程で発音される 
          ・_/(発音開始時ピピッチベンドアップ)の場合は / の後に ピッチベンド音符長(ピッチベンド開始から元の音程に完全移行するまでの時間)を指定できる 、例えば _2/4D2  の場合は Cで発音後に4分音符の長さをかけてピッチベンドアップして、残りの4分音符がDの音程で発音される 
          ・_/(発音開始時ピピッチベンドアップ)の場合は 差分音数やピッチベンド音符数の記載は省略可(未記載時のデフォルトの差分音数は1、ピッチベンド音符数が対象ノードの音符数の50%)

          ・_\(発音開始時ピピッチベンドダウン)の場合は _ の後に 発音開始時のピッチ差分音数を設定できる(1が半音とし、2が全音等で指定)、例えば _2\D の場合は Eで発音後にピッチベンドダウンして、最終的にDの音程で発音される 
          ・_\(発音開始時ピピッチベンドダウン)の場合は \ の後に ピッチベンド音符長(ピッチベンド開始から元の音程に完全移行するまでの時間)を指定できる 、例えば _2\4D2  の場合は Eで発音後に4分音符の長さをかけてピッチベンドダウンして、残りの4分音符がDの音程で発音される 
          ・_\(発音開始時ピピッチベンドダウン)の場合は 差分音数やピッチベンド音符数の記載は省略可(未記載時のデフォルトの差分音数は1、ピッチベンド音符数が対象ノードの音符数の50%)


          ・/_(発音終了時ピッチベンドアップ)の場合は / の後ろに 発音終了時のピッチ差分音数を設定できる(1が半音とし、2が全音等で指定)、例えば C/2_ の場合は Cで発音し、発音終了前にピッチベンドアップして、最終的にDの音程で発音される 
          ・/_(発音終了時ピッチベンドアップ)の場合は _ の後に ピッチベンド音符長(ピッチベンド開始から元の音程に完全移行するまでの時間)を指定できる 、例えば C2/2_4  の場合は 全2分音の中でCで4分音符分を発音後に残りの4分音符の長さをかけてピッチベンドアップして、Dの音程で発音される 
          ・/_(発音終了時ピピッチベンドアップ)の場合は 差分音数やピッチベンド音符数の記載は省略可(未記載時のデフォルトの差分音数は1、ピッチベンド音符数が対象ノードの音符数の50%)

          ・\_(発音終了時ピッチベンドダウン)の場合は \ の後ろに 発音終了時のピッチ差分音数を設定できる(1が半音とし、2が全音等で指定)、例えば D\2_ の場合は Dで発音し、発音終了前にピッチベンドダウンして、最終的にCの音程で発音される 
          ・\_(発音終了時ピッチベンドダウン)の場合は _ の後に ピッチベンド音符長(ピッチベンド開始から元の音程に完全移行するまでの時間)を指定できる 、例えば D2\2_4  の場合は 全2分音の中でDで4分音符分を発音後に残りの4分音符の長さをかけてピッチベンドダウンして、Cの音程で発音される 
          ・\_(発音終了時ピピッチベンドダウン)の場合は 差分音数やピッチベンド音符数の記載は省略可(未記載時のデフォルトの差分音数は1、ピッチベンド音符数が対象ノードの音符数の50%)


          ・ピッチベンド記法は FM音色パート、PCM音色パート設定が long 、の場合のみに設定可能で、PCM音色パートが oneShot の場合には設定不可である
          ・ピッチベンド対象ノートにスタッカート設定されていた場合はピッチベンド音符長もスタッカート設定値に合わせて短くする
          ・ピッチベンド対象ノートがコード(和音)の場合もピッチベンド可能
          ・従来記法同様に空白、スペースは無視される


          (例)

          L2 _2/4 C2 R4 C1 \4_2

          L1 _/[C E G] 

* **完了条件（Definition of Done）**:
  * FM音源設定で新規追加波形をぞれぞれ選択して音色出力に反映されること(既存サイン波の音色を選択した場合に影響がないこと)
    FM音色パート、PCM音色パート(long)のmute(ミュート)設定で対象パートがミュートされること
    新規に追加したポルタメント、ピッチベンドアップ、ピッチベンドダウンのMML記法内容通りにFM音源音色、PCM音色(long)が発音されること
  


### Phase 6: スタンダードMIDIファイルを読み込んで当MMLコマンド形式に変換してMMLファイルを出力するツール作成
* **目的**: スタンダードMIDIファイルを読込み、解析して当エンジンのMML仕様に準じたデータ変換を行い、MMLファイルとしてファイル出力を行なうツール(main関数を持つアプリ)を作る。(当エンジンとは別のツールとして)


          ** スタンダードMIDIファイルから当エンジンMMLコマンド仕様への変換について **
            ・当変換ツールの実行ファイル名は Smf2GoFMML
            ・読み込むスタンダードMIDIファイルはFormat1
            ・当変換ツールはtools配下に変換ツール用のフォルダ(smf2gofmml)を作り、main関数をもつgoファイルをそこに配置して起点とする(各処理goファイルはその下階層にフォルダ(パッケージ)を作ってそちらに配置)
            ・当変換ツールの起動引数には ファイルパス(スタンダードファイルのパス) を受ける(フルパスもしくは実行ファイルがあるフォルダの1個下層のsmfフォルダを読み込み対象とする)
            ・当変換ツール実行後に作成されたMMLファイルは実行ファイルがあるフォルダの1個下層のconvertフォルダを出力対象とする)、フォルダが無い場合は作成
            ・当変換ツール実行後に出力するMMLファイル名は 「曲名.mml」
            ・当変換ツール実行時にエラーが出た場合はコンソールにエラー理由を出力する
            ・読み込んだスタンダードMIDIファイルのテンポ情報を当MML設定[global]の tempo に設定する(小数点は無視)
            ・読み込んだスタンダードMIDIファイルの曲名情報を当MML設定[global]の sequenceID に設定する
            ・読み込んだスタンダードMIDIファイルから取得できない[global]にはデフォルト値を設定(loop: false, volume: 100, reverb: falae, reverbType: normal, reverbTime: 2.0, reverbLevel: 100)               
            ・読み込んだスタンダードMIDIファイルの各トラック名の先頭に[FM]文字列がある場合は、対象トラックをFM音色のパートとする
            ・読み込んだスタンダードMIDIファイルの各トラック名の先頭に[PCM oneShot]文字列がある場合は、対象トラックをPCM音色のoneShotパートとする
            ・読み込んだスタンダードMIDIファイルの各トラック名の先頭に[PCM long]文字列がある場合は、対象トラックをPCM音色のlongパートとする
            ・読み込んだスタンダードMIDIファイルの各トラック名の先頭に[FM][PCM long][PCM oneShot]文字列がない場合はFM音色パートとする
            ・読み込んだスタンダードMIDIファイルのトラックにノートデータが無い場合はそのトラックを無視する
            ・読み込んだスタンダードMIDIファイルの各トラックからMML形式へのパートはSMFのトラック順を元にそれぞれのpartNoに設定する()
            ・読み込んだスタンダードMIDIファイルの各トラックの頭データに CC(コントロールチェンジ) 7番が設定されている場合は　対象パートの音量にその値を設定する
            ・読み込んだスタンダードMIDIファイルの各トラックの頭データに CC(コントロールチェンジ) 10番が設定されている場合は　対象パートのパン(定位)にその値を設定する(127段階から32段階へ変換)
            ・読み込んだスタンダードMIDIファイルのトラックが[PCM oneShot]の場合はノート音階をそれぞれ分解して oneShot 用のMML書式に変換する。
            ・読み込んだスタンダードMIDIファイルのノート音階は MMLの音階と同一で、　音の長さは 当MMLの音の長さ(全音、スタッカート4段階)に吸着変換する
            ・読み込んだスタンダードMIDIファイルの各トラックデータでコード(和音)で長さが違うノートが含まれていた場合は一番長い音に吸着統一(コード内で符割が異なる場合はコード先頭以外のノートは無視)
            ・読み込んだスタンダードMIDIファイルの各トラックデータ を元に　当MMLコマンドの O,L,<.>,Q, R, #, & 等に最適に変換する
            ・読み込んだスタンダードMIDIファイルのベロシティーは無視して V127固定
            ・読み込んだスタンダードMIDIファイルのトラックデータの頭部以外のヴォリュームやパン、その他コントロールデータ(エクスプレッション、ピッチベンド、ポルタメント等)は無視する
            ・出力するMMLデータでSMFからは取得できないデータについてはデフォルト値で埋める(voiceID: 0、volume: 100, reverbSend: 0、mute: false 等)


* **完了条件（Definition of Done）**:
  * スタンダードMIDIファイルを読み込んで、当MMLコマンド仕様のデータが作成され、MMLファイルが出力されること



### Phase 7: 追加機能の実装
* **目的**: 不足している機能などを追加実装する

          ・MMLデータを文字列として入力するメソッド実装：　MML（Music Macro Language）テキスト内容をGolangの文字列として引数を受けるメソッド
              従来ファイルから入力していたデータを文字列で受ける違いはあるが、その後の内部処理は従来と同一
              SetMMLTextData(data string)

          ・FM音色データを文字列として入力するメソッド実装：　FM音音色設定テキスト内容をGolangの文字列として引数を受けるメソッド
              従来ファイルから入力していたデータを文字列で受ける違いはあるが、その後の内部処理は従来と同一
              SetFMVoiceData(data string)

          ・PCM音色データを文字列として入力するメソッド実装：　PCM音色設定テキスト内容をGolangの文字列として引数を受けるメソッド
              従来ファイルから入力していたデータを文字列で受ける違いはあるが、その後の内部処理は従来と同一
              SetPCMVoiceData(data string)

            尚、上記3つの MMLデータ、FM音色データ、PCM音色データのgolang文字列データは ``で囲んだ改行付きの文字列を想定している(テキストファイルと同様の見た目)


          ・マスターボリューム設定メソッドを追加実装する(1): [global]のvolume値を引数の実値で再設定(上書き)するメソッド、曲の再生前、再生中でも逐次設定可
            (再生中に変更した場合は0.3秒程度で音量の大小の差分を滑らかに音量変化させるする)

          ・マスターボリューム設定メソッドを追加実装する(2): 元の[global]のvolume値を100%として、パーセント値を引数で受け、 そのパーセント値で実値を計算してで再設定(上書き)するメソッド、曲の再生前、再生中でも逐次設定可
            (再生中に変更した場合は0.3秒程度で音量の大小の差分を滑らかに音量変化させるする)


          ・フェードイン再生メソッドを追加実装する: 従来のPlayメソッドとは別にFadeInPlayメソッドを実する、引数に秒数(小数一桁まで指定可)を指定し、再生開始時点でvolumeは0、
            指定した秒数をかけて[global]のvolume値までスムーズに音量をフェードインして音量を上げていく

          ・フェードアウト再生メソッドを追加実装する: 従来のPlayメソッドとは別にFadeOutPlayメソッドを実する、引数に秒数(小数一桁まで指定可)を指定し、曲が終わる前の指定秒数に達したら、
            指定した秒数をかけてスムーズに音量をフェードアウトしてvolumeを0にする(0になった時点で再生停止する)(loopがtrueの曲の場合はフェードアウトはしない)

          ・フェードインアウト再生メソッドを追加実装する: フェードイン再生、フェードアウト再生の組み合わせ、引数に秒数(小数一桁まで指定可)を指定し、フェードイン再生とフェードアウト再生を行なう
              (loopがtrueの曲の場合はフェードアウトはしない)

          ・フェードアウトメソッドを追加実装する: 引数に秒数(小数一桁まで指定可)を指定し、再生中に(任意のタイミングで) 指定した秒数をかけてフェードアウトしボリュームが０になった時点で再生停止する(曲の終わりに関係なく)
              (loopがtrueの曲の場合でもフェードアウトする)


          ・既存のStop,Pause、Resume、メソッド修正: それぞれ各メソッドで曲の再生、停止、再開をする際に0.1秒程度の短いフェードをかけて曲の停止や再開時に途切れなどの違和感が出ない様にする

          ・MML仕様にコンダクターパートの仕様を新規追加: コンダクターパートはMMLに1パートだけ設置可能で、ループ時の再生開始ジャンプポイント設定や　曲全体のテンポ変化等を設定するパート(コンダクターパート自体の記載は無くても可)
              [conductor]タグの下に記載し、[conductorPart]と[conductorPartEnd]の間にコンダクターパートコマンドを記載する(空白、空行は無視する)
              コンダクターパートの先頭にテンポが記載している場合は[global]のテンポを無視して、コンダクターパートのテンポを優先する
              コンダクターパートの曲途中にテンポが記載している場合は他パートを含め曲全体のテンポを逐次変更する
              [global]の loopが true の曲で ジャンプポイント を設定している場合は 再生開始時は先頭(もしくはstartOffset値位置)から再生し、曲の末尾に達した際に ジャンプポイントの位置からループ再生される(以降はその範囲でループ)
                この機能は 曲のイントロは1回のみ再生され、その後は繰り返し箇所を再生するという使い方になります
                ジャンプポイントはコンダクターパート内に1個のみ記載可(複数記載の場合は2個目以降は無視)


              コンダクターパートコマンド仕様
                T: Tの後ろにテンポを記載する(小数点は無視)、例 T120
                R : 休符（レスト）
                数字: 休符の後につけ、長さを表す。2=2分音符。.は付点で長さを1.5倍し、4.=付点4分音符 12=3連符
                J : ジャンプポイント


              コンダクターパート記載例
              [conductor]
              [conductorPart]
                R1 R1 J
                T120 R4 T115 R4 T110 R4
                T80
              [conductorPartEnd]


        ・PCM波形データの効率化対応: LoadPCMVoiceFile, LoadPCMVoiceFileFS, SetPCMVoiceDataの各メソッド内処理で PCM波形を読み込む際は既に同じWAVファイルがメモリ内に保持している場合は
              再度読み込まずにメモリ内のPCM波形を流用する、メモリ内に無いWAV波形は読み込む
              (PCM波形以外のパラメータは上書き)

        ・PCM波形データクリアメソッドの新規追加: メモリ内に保持しているPCM波形を全クリアするメソッドを新規追加する

        ・FM音色、PCM音色の設定パラメータにLFO(ビブラート)項目を追加する(LFOパラメータは無記載でも可)、lfoをtrueにした場合で、各lfoパラメータが未設定の場合はデフォルト値を使用する
            PCM音色でLFOを掛けられるのは voiceType が long タイプのみとする(oneShotの場合は無視)

            lfo: trueの場合はLFOを有効にする、falseの場合はLFOを無効にする
            lfoDelay : 発音してからLFOがかかるまでの秒数(0.1秒単位の数値)、デフォルト値は 0.4
            lfoFade : LFO開始からLFO Depthが最大になるまでの秒数(0.1秒単位の数値)、 デフォルト値は 0.5
            lfoDepth: LFOの深さを設定(単位はCents、100=半音1個分)、 デフォルト値は 30
            lfoHz: LFOの周期を設定(単位はHz、7=7Hz)、デフォルト値は 6


            fmVoice:
              - voiceID: 0
                algorithm: 4
                feedback: 20
                lfo: true
                lfoDelay: 0.3
                lfoFade: 0.4
                lfoHz: 7

            pcmVoice:
              - voiceID: 1
                voiceType: long
                loop: true
                lfo: true
                lfoDelay: 0.3
                lfoFade: 0.4
                lfoHz: 7
                voiceSetting:
                  - fileName: uhh.wav
                    note:
                    baseNote: A3
                    volume: 65
                    pan: 16
                    envelope: "31, 1, 2, 6, 14"


          ** MMLファイル追加仕様(ソロ再生機能) **
            FM音色パート、PCM音色パートに solo パラメータを新規追加、solo を trueにした場合は 対象パートのみ再生し、他のパートは自動でミュートにする(複数パートのsolo設定可)
              (主にデバック用に使用する機能である)

             solo: true (ソロ再生を使用する)
             solo: false (ソロ再生を使用しない)

            (未指定 or 無効値の場合はソロせずに通常再生)

  
            [partSetting]
            part:
              - partNo: 1
                voiceID: 10
                volume: 50
                pan: 0
                transpose: -12
                mute: true
                solo: true

            [pcmPartSetting]
            pcmPart:
              - partNo: A
                voiceID: 0
                partType: long
                volume: 60
                pan: 0
                transpose: 2
                mute: false
                solo: false


* **完了条件（Definition of Done）**:
  * ボリュームフェード機能、コンダクターパート機能、PCM波形効率化、音色パラメータにLFO設定追加、の各追加機能や修正箇所の実装が正しく動作すること


### Phase 8: ディレクトリ構成の整理、仕様・使用方法・ライセンス ドキュメント作成
* **目的**: ディレクトリ構成を見直しと整理を行なう、また公開メソッドや音源パラメータ仕様、MMLコマンド仕様等や使い方のHTMLや ライセンスドキュメントを出力する。


  * ディレクトリ構成を見直しについて

    ・リポジトリトップディレクトリ名が現状「fm-driver」になっていますが「go-fmml」に変更(リファクタリグ)する
      (当サウンドエンジンの呼称をgo-fmmlとする)

    ・実装ソースの配置場所を　srcディレクトリに 配置(リファクタリグ)する
    ・トップディレクトリ配下の docs ディレクトリ配下には 後述する HTMLファイル を配置する
    ・トップディレクトリ直下 にはファイル ライセンスドキュメント を配置する

      ```
      go-fmml/
        ├── LICENSE              <-- コード用のMITライセンス全文
        ├── LICENSE-DOCS         <-- ドキュメント用のCC BY 4.0全文
        ├── README.md            <-- ライセンスの概要・デュアルライセンスの明記
        ├── CLAUDE.md
        ├── docs/
        │   └── index.html      <--  HTMLドキュメント
        └── src/                 <-- ソースコード群
      ```

      ※ 【Phase 8完了後の追加修正】上記では実装ソースを `src/` ディレクトリ配下に配置する方針だっ
      たが、`go.mod` がリポジトリ直下ではなく `src/go.mod` にある一方でモジュールパスは
      `github.com/megaak-soft/go-fmml`（サブディレクトリ無し）のままだったため、GitHub公開後に
      外部ユーザーが `go get`/`import` で本ライブラリを取得しようとした際にモジュール解決できない
      問題が判明した。これを解消するため、`go.mod` および全ソースコード（`fmcore/`, `pcmcore/`,
      `player/`, `fileio/`, `analyze/`, `memory/`, `common/`, `const/`, `reverb/`, `cmd/`, `tools/`
      等）を `src/` から**リポジトリ直下**へ移動し、`src/` ディレクトリは廃止した。以降の実際の
      ディレクトリ構成は以下の通り。

      ```
      go-fmml/
        ├── LICENSE              <-- コード用のMITライセンス全文
        ├── LICENSE-DOCS         <-- ドキュメント用のCC BY 4.0全文
        ├── README.md            <-- ライセンスの概要・デュアルライセンスの明記
        ├── CLAUDE.md
        ├── go.mod, go.sum       <-- Goモジュール定義（リポジトリ直下）
        ├── docs/
        │   └── index.html      <--  HTMLドキュメント
        ├── fmcore/, pcmcore/, player/, fileio/, analyze/, memory/,
        │   common/, const/, reverb/  <-- ソースコード（パッケージ）
        ├── cmd/                 <-- 動作確認用デモアプリ
        └── tools/               <-- 付属ツール（Smf2GoFMML）
      ```


  * ライセンスについて

    ・ソースコードについてはAIによるコーディングで作成したものであるので、幅広く自由に使ってもらう為に、MITライセンスを適用し、
      著作権表示とライセンス全文を表示（保持）するだけで、商用・個人問わずほぼ無制限に利用・改変・再配布が可能　とします
      トップディレクトリ直下 の LICENSE テキストファイルに コード用のMITライセンス全文内容 を　日本語、英語 で出力お願いします

    ・CLAUDE.md およびHTML等ドキュメントについては、著者が時間をかけて仕様設計、記載した内容であるので 著作権を主張し、CC BY 4.0(クリエイティブコモンズ)ライセンスを適用します
      トップディレクトリ直下 の LICENSE-DOCS テキストファイルに ドキュメント用のCC BY 4.0全文内容 を　日本語、英語 で出力お願いします

    ・go-fmml の著作者は「MEGAAK SOFT」とします。
    

    ・go-fmmlに関連する全ソースコードの先頭ヘッダに以下 MITライセンスコメントの付加をお願いします
    ```golang
    /*
    * Copyright (c) 2026 MEGAAK SOFT (https://megaak.com)
    * SPDX-License-Identifier: MIT
    * 
    * Full license text is available in the LICENSE file at the root of this repository.
    */

    ```
    
    ・go-fmmlに関連するHTML以外のテキストドキュメントすべてに以下フッターを付加をお願いします

    ```
    ---
    ## License

      © 2026 MEGAAK SOFT (https://megaak.com) — 
      本ドキュメントは CC BY 4.0　の下でライセンスされています。
      https://creativecommons.org/licenses/by/4.0/deed.ja

    ```

    go-fmmlに関連するHTMLページすべてに以下フッターを付加をお願いします
    ```:html

      <footer>
        <hr>
        <p>
          © 2026 <a href="https://megaak.com">MEGAAK SOFT</a> — 
          本ドキュメントは <a href="https://creativecommons.org/licenses/by/4.0/deed.ja" target="_blank" rel="noopener">CC BY 4.0</a> の下でライセンスされています。
        </p>
      </footer>

    ```



  * go-fmml 説明ドキュメント(HTML等)について
    以下ドキュメント作成をお願いします

    ・README.md : go-fmmlについての概要文のひな型作成やライセンスの概要・デュアルライセンスの明記をお願いします(あとで追記・編集します)
      プルリクエストは受け付けない旨の以下文言を記載する
        日本語: このプロジェクトは個人プロジェクトであり、プルリクエストや外部からの貢献は受け付けていません。ご理解いただけますと幸いです。
        英語: Currently, this project is a personal project and does not accept Pull Requests or external contributions. Thank you for your understanding!

    ・CONTRIBUTING.md: READMEに記載したプルリクエストは受け付けない旨の文言を日本語と英語で記載

    ・HTMLを出力する場所は go-fmml/docsディレクトリ

    ・go-fmml についての説明文。何ができるのか、どういう機能スペック、負荷パフォーマンスがどれくらいか、ebitengine/oto/v3との関係(oto/v3のライセンス表記等)
      説明文にはClaude codeで仕様書駆動開発で実装した旨を記載
      go-fmmlのアイコンは img/go-fmml_icon.jpg を使用(画面内のヘッダ等で表示)
      GPL汚染を排除を考慮した実装(AIにチェックさせて)をしていることの記載
      go-fmmlのライセンスがMITで商用、配布、組み込み等の条件を記載(メニューで別ページに表示したほうがいいかも)、ただし当ドキュメントはクリエイティブコモンズライセンスであることを明記

    ・index.htmlに作者についての簡単な紹介を記載「MEGAAK SOFT」はインディーズゲーム開発個人プロジェクト、レトロ風2Dゲームを作っています」の文章と主に各主URLのリンクを設置(アイコンはimg/megaak_icon.jpg を使用)
      HP: https://megaak.com
      GitHub: https://github.com/megaak-soft/go-fmml
      X: https://x.com/MegaakSoft

    ・公開メソッド仕様一覧HTML : 各種設定ファイル読込、再生、ストップなどのユーザーに公開するメソッド仕様と簡単な概要を記載

    ・FM音色パラメータ仕様HTML: 記載方法、設定可能範囲値、アルゴリズム図、設定例などを記載

    ・PCM音色パラメータ仕様HTML: 記載方法、設定可能範囲値、voiceTypeの oneShot, longの説明、設定例などを記載

    ・MMLパートMML仕様HTML: [global][conductor][partSetting][pcmPartSetting]の記載方法や 各種パラメータ設定可能範囲値、設定例などを記載

    ・MMLコマンド仕様HTML: コンダクターパートコマンド、FM音色パートコマンド、PCM音色パートコマンド(oneShot, long)のコマンド仕様一覧、記載方法、設定可能範囲値、設定例などを記載

    ・golangアプリでgo-fmmlを利用する為のコードサンプルを記載(ebitengine/oto/v3のコンテキストと併用利用する際のコード例等も記載)


* **完了条件（Definition of Done）**:
  * ディレクトリ構成のリファクタリングが想定通りであること、ライセンス表記ドキュメント・go-fmml説明HTMLドキュメントの作成されること、既存コードにライセンスコメントが追加されること


---
## License

Copyright (c) 2026 MEGAAK SOFT (https://megaak.com)
This document (`CLAUDE.md`) is licensed under a [Creative Commons Attribution 4.0 International License (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/).